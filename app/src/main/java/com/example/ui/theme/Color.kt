package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Premium Studio Palette ──────────────────────────────────────────────────
// Background / Canvas
val BackgroundLight      = Color(0xFFF2F4F9)   // Cool periwinkle-slate page
val SurfaceLight         = Color(0xFFFFFFFF)   // Pure white cards
val SurfaceVariantLight  = Color(0xFFEBEEF7)   // Soft indigo-tinted container

// Typography
val TextPrimaryLight     = Color(0xFF0D1B3E)   // Deep navy (max contrast)
val TextSecondaryLight   = Color(0xFF4A5578)   // Muted indigo-slate
val TextTertiaryLight    = Color(0xFF8B96B8)   // Soft hint text

// Borders
val BorderLight          = Color(0xFFD5DAF0)   // Indigo-tinted border
val CardBorderLight      = Color(0xFFE8ECFA)   // Subtle card separator

// ── Primary Action ──────────────────────────────────────────────────────────
val PrimaryBlack         = Color(0xFF1A1F4B)   // Deep midnight-indigo button
val AccentBlack          = Color(0xFF0D1035)   // Ultra-dark for pressed state

// ── Vibrant Accents ─────────────────────────────────────────────────────────
val AccentBlue           = Color(0xFF4361EE)   // Vibrant Royal Indigo
val AccentBlueBg         = Color(0xFFE0E7FF)   // Light indigo container
val AccentPurple         = Color(0xFF7C3AED)   // Electric Violet
val AccentPurpleBg       = Color(0xFFEDE9FE)
val AccentGreen          = Color(0xFF06B27E)   // Mint Emerald
val AccentGreenBg        = Color(0xFFCCFBF1)
val AccentOrange         = Color(0xFFFC7C33)   // Warm Tangerine
val AccentOrangeBg       = Color(0xFFFFEDD5)
val AccentRed            = Color(0xFFE53E3E)   // Bold Crimson
val AccentRedBg          = Color(0xFFFEE2E2)
val AccentAmber          = Color(0xFFF59E0B)   // Warm Amber
val AccentPink           = Color(0xFFEC4899)   // Hot Pink

// ── Gradient Pairs (start -> end) ────────────────────────────────────────────
val GradStart            = Color(0xFF4361EE)   // Indigo gradient start
val GradEnd              = Color(0xFF7C3AED)   // Violet gradient end
val GradWarmStart        = Color(0xFFFC7C33)   // Orange warm start
val GradWarmEnd          = Color(0xFFEC4899)   // Pink warm end

// ── Dark scheme (proper dark palette) ───────────────────────────────────
val DarkBackground       = Color(0xFF0F1117)   // Near-black page
val DarkSurface          = Color(0xFF1A1D2E)   // Dark navy cards
val DarkSurfaceVariant   = Color(0xFF252A3A)   // Muted dark container
val DarkTextPrimary      = Color(0xFFF1F5F9)   // Light text
val DarkTextSecondary    = Color(0xFF94A3B8)   // Muted light
val DarkTextTertiary     = Color(0xFF64748B)   // Dim hint text
val DarkBorder           = Color(0xFF2D3348)   // Dark border
val DarkCardBorder       = Color(0xFF1E2235)   // Dark card separator

// Dark accent backgrounds (muted versions for containers)
val DarkAccentBlueBg     = Color(0xFF1E2A4A)
val DarkAccentPurpleBg   = Color(0xFF2A1E4A)
val DarkAccentGreenBg    = Color(0xFF0A2E22)
val DarkAccentOrangeBg   = Color(0xFF3A2010)
val DarkAccentRedBg      = Color(0xFF3A1015)
val DarkPrimaryBlack     = Color(0xFFE2E8F0)   // Light button in dark mode
